# a simple recursive function

# Output:

# Program:

begin
  int rec(int x) is
    println x;
    if x == 0
    then
      skip
    else

      int y = call rec(x - 1)
    fi ;
    return 42
  end
 
  int x = 0 ;
  x = call rec(8);

  println x
end
