stages:
  - build
  - test

build-job:
  stage: build
  script: 
    - ls
    - make

test_job:
  stage: test
  script:
    - scala-cli test .
  artifacts:
    paths:
      - test_output.txt
  allow_failure: true
