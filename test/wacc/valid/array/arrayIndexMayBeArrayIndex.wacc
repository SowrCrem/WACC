# Testing recursive array indexing

# Output:
# 6
# 7
# 8
#

# Program:

begin
   # int[] idxs1 = [2, 0, 1] ;
   # int[] idxs2 = [1, 2, 0] ;
   # # idxs1[idxs2[0]] = 0
   # # idxs1[idxs2[1]] = 1
   # # idxs1[idxs2[2]] = 2
   # int[] xs = [5, 6, 7] ;
   # println xs[0];
   # int i = 0 ;
   # while i != 3 do
   #     int first = idxs2[i];
   #     int index = idxs1[first];
   #     println index;
   #     xs[index] = xs[index] + 1 ;
   #     println (xs[index]) ;
   #     i = i + 1
   # done

   int[] a = [2, 0, 1];
   int[] b = [1, 2, 0];
   int[][] c = [a, b];
   int[] xs = [5, 6, 7];
   int i = 0;
   while i != 2 do 
        int first = b[i];
        println first;
        int index = a[first];
        println index;
        xs[index] = 1;
        println index;
        i = i + 1
   done;

   int[] d = c[0]

   
end
