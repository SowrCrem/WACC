package wacc


object MacroExpander {

    val pos = (0,0)
    def expandMacros(prog: Program): Program = {
        prog match {
            case Program(macroList, funcList, statList) => {
                Hashhh
aS
                prog
            }
        }
    }
}